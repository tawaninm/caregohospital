# 05 Role Rules

VoiceMed B2C roles:

- `owner`: account owner and payer.
- `caregiver`: can view/manage elder care settings.
- `viewer`: can view summaries and alerts.

Prototype uses local mock auth. Future backend must enforce permissions server-side.

Do not bring back hospital roles as the primary UX.
